# jaras Brand Guidelines Webpage

Static bilingual webpage version of the jaras brand guidelines. Arabic is the default view, with an English switcher in the header. The page includes a complete inline guideline deck rendered from all 31 pages of the original PDF.

Open `index.html` in a browser, or run a local server from this folder:

```bash
python3 -m http.server 8080
```

The original guideline PDF is available at `assets/jaras_brand_guidelines.pdf`.
